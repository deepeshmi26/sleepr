apiVersion: v1
kind: Service
metadata:
  labels:
    app: notifications
  name: notifications
spec:
  ports:
  - name: "tcp"
    port: 4000
    protocol: TCP
    targetPort: 4000
  selector:
    app: notifications
  type: ClusterIP
