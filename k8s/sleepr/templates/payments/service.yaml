apiVersion: v1
kind: Service
metadata:
  labels:
    app: payments
  name: payments
spec:
  ports:
  - name: "tcp"
    port: 4000
    protocol: TCP
    targetPort: 4000
  selector:
    app: payments
  type: ClusterIP
